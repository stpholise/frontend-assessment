// interface PaginationProps {
//   currentPage: number;
//   totalPages: number;
//   maxVisible?: number;
// }

// const Pagination = ({ currentPage, totalPages, maxVisible = 6 }: PaginationProps): (number | "...") => {
// const range : (number | "...")

// if (totalPage <= maxVisible) {
//   for (let i = 0; i < totalPages; i++) range.push(i)
// }

//     return <div>Pagination</div>;
// };

// export default Pagination;
